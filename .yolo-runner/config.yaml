default_profile: tk
profiles:
  tk:
    tracker:
      type: tk
  github:
    tracker:
      type: github
      github:
        scope:
          owner: egv
          repo: yolo-runner
        auth:
          token_env: GITHUB_TOKEN
agent:
  backend: codex
  model: gpt-5.3-codex
