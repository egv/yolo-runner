# Installation Test Matrix

This matrix defines the install-path validation scenarios for source builds, release artifacts, and installation scripts across supported platforms.

| Platform | Architecture | Shell notes | make install command | make install success | release artifact command | release artifact success | install script command | install script success |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| macOS | amd64 | bash / zsh (`sh`-compatible) | `make install` | command exits 0; `bin/yolo-agent --help` prints usage | `curl -fsSL -o /tmp/yolo-runner-darwin-amd64.tar.gz https://github.com/egv/yolo-runner/releases/latest/download/yolo-runner_darwin_amd64.tar.gz && tar -xzf /tmp/yolo-runner-darwin-amd64.tar.gz -C /tmp && /tmp/yolo-runner --help` | command exits 0; extracted binary prints usage and `sha256sum` matches a release manifest | `curl -fsSL https://raw.githubusercontent.com/egv/yolo-runner/main/install.sh \| bash` | command exits 0; `yolo-agent --version` works from PATH after script run |
| macOS | arm64 | bash / zsh (`sh`-compatible) | `make install` | command exits 0; `bin/yolo-agent --help` prints usage | `curl -fsSL -o /tmp/yolo-runner-darwin-arm64.tar.gz https://github.com/egv/yolo-runner/releases/latest/download/yolo-runner_darwin_arm64.tar.gz && tar -xzf /tmp/yolo-runner-darwin-arm64.tar.gz -C /tmp && /tmp/yolo-runner --help` | command exits 0; extracted binary prints usage and `sha256sum` matches a release manifest | `curl -fsSL https://raw.githubusercontent.com/egv/yolo-runner/main/install.sh \| bash` | command exits 0; `yolo-agent --version` works from PATH after script run |
| Linux | amd64 | bash (`sh` + `bash`) | `make install` | command exits 0; `bin/yolo-agent --help` prints usage | `curl -fsSL -o /tmp/yolo-runner-linux-amd64.tar.gz https://github.com/egv/yolo-runner/releases/latest/download/yolo-runner_linux_amd64.tar.gz && tar -xzf /tmp/yolo-runner-linux-amd64.tar.gz -C /tmp && /tmp/yolo-runner --help` | command exits 0; extracted binary prints usage and `sha256sum` matches a release manifest | `curl -fsSL https://raw.githubusercontent.com/egv/yolo-runner/main/install.sh \| bash` | command exits 0; `yolo-agent --version` works from PATH after script run |
| Linux | arm64 | bash (`sh` + `bash`) | `make install` | command exits 0; `bin/yolo-agent --help` prints usage | `curl -fsSL -o /tmp/yolo-runner-linux-arm64.tar.gz https://github.com/egv/yolo-runner/releases/latest/download/yolo-runner_linux_arm64.tar.gz && tar -xzf /tmp/yolo-runner-linux-arm64.tar.gz -C /tmp && /tmp/yolo-runner --help` | command exits 0; extracted binary prints usage and `sha256sum` matches a release manifest | `curl -fsSL https://raw.githubusercontent.com/egv/yolo-runner/main/install.sh \| bash` | command exits 0; `yolo-agent --version` works from PATH after script run |
| Windows | amd64 | PowerShell 7+ (`pwsh`) | `make install` | command exits 0; `yolo-agent.exe --help` prints usage | `$tag = (Invoke-RestMethod https://api.github.com/repos/egv/yolo-runner/releases/latest).tag_name; Invoke-WebRequest -OutFile \"$env:TEMP\\yolo-runner-windows-amd64.zip\" \"https://github.com/egv/yolo-runner/releases/download/$tag/yolo-runner_windows_amd64.zip\"; Expand-Archive -Path \"$env:TEMP\\yolo-runner-windows-amd64.zip\" -DestinationPath $env:TEMP -Force; & \"$env:TEMP\\yolo-runner.exe\" --help` | command exits 0; extracted binary prints usage and `Get-FileHash` matches release manifest | `iwr https://raw.githubusercontent.com/egv/yolo-runner/main/install.ps1 -UseBasicParsing | iex` | command exits 0; `yolo-agent.exe --version` works from PATH after script run |
